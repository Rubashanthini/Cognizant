# common-library is a plain shared Java library (DTOs only) and is not an
# executable Spring Boot application, so it does not require Spring Boot
# configuration properties of its own.
#
# This file is intentionally kept minimal and exists only to satisfy the
# convention that every module in this multi-module project ships an
# application.properties file alongside its sources.
spring.application.name=common-library
